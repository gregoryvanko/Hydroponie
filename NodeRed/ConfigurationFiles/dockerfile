FROM nodered/node-red:latest

WORKDIR /data
COPY package.json /data
RUN npm install --unsafe-perm --no-update-notifier --no-fund --only=production --omit=dev
WORKDIR /usr/src/node-red


USER root
RUN apk add py3-rpigpio

USER node-red